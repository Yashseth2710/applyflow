# ApplyFlow backend — development dependencies
-r requirements.txt

# Testing
pytest==9.1.1
pytest-asyncio==1.4.0
pytest-cov==7.1.0
httpx==0.28.1

# Linting / typing / formatting
ruff==0.16.3
mypy==2.3.0

# Known vulnerabilities in the pinned dependencies. Also installed separately
# in CI, so `pip-audit -r` there reads these files rather than whatever the
# runner happens to have.
pip-audit==2.10.1
