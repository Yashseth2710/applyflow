# ApplyFlow backend — runtime dependencies

# Web framework
fastapi==0.141.1
uvicorn[standard]==0.52.1
python-multipart==0.0.32

# Validation / settings
pydantic==2.13.4
pydantic-settings==2.15.0
email-validator==2.3.0

# Database
sqlalchemy==2.0.51
alembic==1.19.0
psycopg[binary]==3.3.4

# Auth
pyjwt==2.13.0
argon2-cffi==23.1.0

# File / document handling
pypdf==6.15.0
# Avatars are re-encoded rather than stored as uploaded. That strips EXIF —
# which carries GPS coordinates on anything taken with a phone — and it means
# what gets stored is definitely an image rather than whatever was claimed.
Pillow==12.3.0

# HTTP client (AI provider calls, Ollama)
httpx==0.28.1

# Rate limiting
slowapi==0.1.10
